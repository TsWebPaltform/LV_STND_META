<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <typeAliases>
        <!-- 여기에 매퍼 인터페이스의 패키지 경로를 설정해주세요 -->
        <package name="com.example.LV_STND_META.mappers" />
    </typeAliases>

    <mappers>
        <!-- 여기에 MenuMapper.xml 파일의 경로를 설정해주세요 -->
        <mapper resource="com/example/LV_STND_META/mappers/MenuMapper.xml" />

    </mappers>
</configuration>
